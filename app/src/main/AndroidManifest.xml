<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mutiaramobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mutiaralauncher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mutiaralauncher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menupage.penerimaanbarang.InputPenerimaanBarangPage"
            android:label="Input Penerimaan Barang"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".menupage.penerimaanbarang.TerimaBarangPage"
            android:label="Terima Barang"
            android:theme="@style/AppTheme.NoActionBar">
            <!--<meta-data-->
                <!--android:name="android.support.PARENT_ACTIVITY"-->
                <!--android:value=".MainActivity" />-->
        </activity>
        <activity
            android:name=".LoginPage"
            android:theme="@style/AppTheme.Fix" />
        <activity
            android:name=".MainActivity"
            android:label="Mutiara Mobile"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>